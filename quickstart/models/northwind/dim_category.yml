version: 2

models:
  - name: dim_category
    description: Category Dimension. One row per product category.
    columns:
      - name: categorykey
        description: Surrogate key for category
        tests:
          - not_null
          - unique

      - name: categoryid
        description: Business key for category
        tests:
          - not_null
          - unique